export default function HymnsSettings() {
  return (
    <div style={{ padding: 16 }}>
      <h3>Hymns Settings</h3>
      <p>
        Hymns uses the same current Service ID as the Service and Sermon tiles.
        Import hymn records into Supabase first, then search and add them here.
      </p>
    </div>
  );
}
